openapi: 3.1.0
info:
  title: Salles API
  version: 1.0.0
servers:
  - url: http://localhost:3000
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
  /auth/register:
    post:
      summary: Inscription
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password, pseudo]
              properties:
                email: { type: string, format: email }
                password: { type: string, minLength: 8 }
                pseudo: { type: string }
      responses:
        '201':
          description: Created
  /auth/login:
    post:
      summary: Connexion
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email: { type: string }
                password: { type: string }
      responses:
        '200':
          description: OK
  /lieux:
    get:
      summary: Liste des lieux
      parameters:
        - { name: type, in: query, schema: { type: string, enum: [blacklist, favori] } }
        - { name: ville, in: query, schema: { type: string } }
        - { name: search, in: query, schema: { type: string } }
        - { name: code_postal, in: query, schema: { type: string } }
        - { name: auteur_id, in: query, schema: { type: integer } }
        - { name: has_coords, in: query, schema: { type: boolean } }
        - { name: page, in: query, schema: { type: integer } }
        - { name: limit, in: query, schema: { type: integer, maximum: 100 } }
        - name: sort
          in: query
          schema:
            type: string
            enum: [updated_at, created_at, nom, ville, date_dernier_evenement]
        - { name: order, in: query, schema: { type: string, enum: [asc, desc] } }
      responses:
        '200':
          description: Liste paginée avec meta (totalPages, hasPrev, hasNext, counts)
  /lieux/map:
    get:
      summary: Points carte (filtres sans pagination, max 500)
      parameters:
        - { name: type, in: query, schema: { type: string, enum: [blacklist, favori] } }
        - { name: ville, in: query, schema: { type: string } }
        - { name: search, in: query, schema: { type: string } }
      responses:
        '200':
          description: Lieux avec latitude/longitude
    post:
      summary: Créer un lieu
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LieuInput'
      responses:
        '201':
          description: Created
  /lieux/{id}:
    get:
      summary: Détail lieu
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        '200':
          description: OK
    put:
      summary: Modifier lieu
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        '200':
          description: OK
    delete:
      summary: Supprimer lieu
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        '204':
          description: No Content
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    LieuInput:
      type: object
      required: [nom, adresse]
      properties:
        nom: { type: string }
        adresse: { type: string }
        type: { type: string, enum: [blacklist, favori] }
        commentaire: { type: string }
        ville: { type: string }
        latitude: { type: number }
        longitude: { type: number }
